import React from 'react'
import { styled, Typography } from "@mui/material"
const StyledTypography = styled(Typography)({
  color: 'blue',
})

export const AllUsersTablePage = () => {
  return <StyledTypography>AllUsersTablePage</StyledTypography>
}
